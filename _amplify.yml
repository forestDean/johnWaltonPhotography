version: 1
frontend:
  phases:
    preBuild:
      commands:
        - php --version
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:

  build:
    commands:
      - php --version
      - php build.php
