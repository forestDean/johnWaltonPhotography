version: 1
frontend:
  phases:
    preBuild:
      commands:
        - php --version
    build:
      commands:
        - php build.php
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - vendor/**/*